<UserControl x:Class="GestionComercial.Desktop.Controls.Sales.SalesControlView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" 
             d:DesignWidth="900" 
             x:Name="miUserControl" 
             Loaded="miUserControl_Loaded" 
             Background="Gray" SizeChanged="miUserControl_SizeChanged">
    <Grid x:Name="GridGeneral" 
          HorizontalAlignment="Stretch" 
          VerticalAlignment="Stretch" 
          Margin="10" 
          Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/> <!-- DataGrid artículos -->
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <!-- Fecha (fila 0, columna 0) -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Grid.Row="0" Grid.Column="0" Margin="10,0,20,8">
            <TextBlock Text="Fecha" VerticalAlignment="Center" Style="{StaticResource BlueBold10}"/>
            <DatePicker x:Name="dpDate" VerticalAlignment="Center" Width="100" />
        </StackPanel>
        <!-- Cliente y detalles (fila 0) -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Margin="10,0,20,8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Cliente" VerticalAlignment="Center" Style="{StaticResource BlueBold10}"/>
                    <TextBox x:Name="txtClientCode" Style="{StaticResource TextBoxSearch}" Width="100" VerticalAlignment="Center" KeyDown="txtClientCode_KeyDown"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1">
                    <TextBox x:Name="txtFansatyName" Style="{StaticResource TextBoxSearch}" Width="250" VerticalAlignment="Center" IsReadOnly="True" Text="{Binding FantasyName}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="2">
                    <TextBox x:Name="txtAddress" Style="{StaticResource TextBoxSearch}" Width="440" Height="60" VerticalAlignment="Center" IsReadOnly="True" Text="{Binding Address}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,2,0,0">
                    <TextBox x:Name="txtEmail" Style="{StaticResource TextBoxSearch}" Width="auto" MinWidth="250" VerticalAlignment="Center" Text="{Binding Email}"/>
                    <CheckBox x:Name="chSendEmail" VerticalAlignment="Center" Style="{StaticResource CheckBox}"/>
                    <TextBlock Text="Enviar Comprobante por mail" VerticalAlignment="Center" Style="{StaticResource BlackNormal10}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
        <!-- Lista de precios y condición de venta (fila 1) -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Margin="10,0,20,8">
            <TextBlock Text="Lista de Precios" VerticalAlignment="Center" Style="{StaticResource BlueBold10}"/>
            <ComboBox x:Name="cbPriceLists" Style="{StaticResource ComboBox}" Width="Auto" DisplayMemberPath="Description" SelectedValuePath="Id" ItemsSource="{Binding PriceLists}" SelectedValue="{Binding PriceListId}" IsEnabled="False"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="2" Margin="10,0,20,8" HorizontalAlignment="Left">
            <TextBlock Text="Condición de Venta" VerticalAlignment="Center" Style="{StaticResource BlueBold10}"/>
            <ComboBox x:Name="cbSaleConditions" Style="{StaticResource ComboBox}" Width="Auto" DisplayMemberPath="Description" SelectedValuePath="Id" ItemsSource="{Binding SaleConditions}" SelectedValue="{Binding SaleConditionId}"/>
        </StackPanel>
        <!-- DataGrid para artículos (fila 2) -->
        <StackPanel x:Name="spArticles" 
                    Orientation="Horizontal" 
                    VerticalAlignment="Top" 
                    Grid.Row="2" Grid.Column="0" 
                    Grid.ColumnSpan="4" 
                    Visibility="Hidden" 
                    Margin="10,0,20,8">
            <StackPanel>
                    <DataGrid x:Name="dgArticles" 
                              ItemsSource="{Binding ArticleItems, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False" 
                              HeadersVisibility="Column" 
                              Margin="5" 
                              Height="300"
                              KeyDown="dgArticles_KeyDown" 
                              CellEditEnding="dgArticles_CellEditEnding" 
                              Loaded="dgArticles_Loaded" 
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Código" Binding="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                            <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="200" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="Lista de Precios" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <!-- Mostrar combo no editable (muestra la descripción) -->
                                        <ComboBox ItemsSource="{Binding PriceLists}" DisplayMemberPath="Description" SelectedValuePath="Id" SelectedValue="{Binding PriceListId, Mode=TwoWay}" IsHitTestVisible="False" Focusable="False"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <!-- Editable: ItemsSource por fila -->
                                        <ComboBox ItemsSource="{Binding PriceLists}" DisplayMemberPath="Description" SelectedValuePath="Id" SelectedValue="{Binding PriceListId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Cantidad" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                            <DataGridTextColumn Header="Precio" Binding="{Binding Price, StringFormat={}{0:C2}}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat={}{0:C2}}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Bonificación (%)" Binding="{Binding Bonification, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat={}{0:C2}}" Width="100" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
             </StackPanel>
        </StackPanel>
        <!-- Checkbox y TextBox para código de barras (fila 3) -->
        <StackPanel x:Name="spPostMethod"
                    Visibility="Hidden"
                    Orientation="Horizontal" 
                    Grid.Row="3" 
                    Grid.Column="1" 
                    Grid.ColumnSpan="2" 
                    Margin="10,0,20,8" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Bottom">
                <CheckBox x:Name="chBarcode" 
                          Content="Habilitar lectura por código de barras" 
                          VerticalAlignment="Center" 
                          Checked="chBarcode_Checked" 
                          Unchecked="chBarcode_Unchecked"/>
                <TextBox x:Name="txtBarcode" 
                         Width="200" 
                         Margin="10,0,0,0" 
                         VerticalAlignment="Center" 
                         KeyDown="txtBarcode_KeyDown"/>
        </StackPanel>

       
        <!-- Botones (fila 6) -->
        <Button x:Name="btnCancel" Grid.Row="6" Grid.Column="1" Content="Cancelar" Margin="0,40,150,0" Click="BtnCancel_Click" Style="{StaticResource BtnCancel}"/>
        <Button x:Name="btnUpdate" Grid.Row="6" Grid.Column="1" Content="Actualizar" Margin="150,40,0,0" Click="BtnUpdate_Click" Style="{StaticResource BtnUpdate}"/>
        <Button x:Name="btnAdd" Grid.Row="6" Grid.Column="1" Content="Guardar" Margin="150,40,0,0" Click="BtnAdd_Click" Style="{StaticResource BtnAdd}"/>
        <TextBlock x:Name="lblError" Grid.Row="7" Grid.ColumnSpan="6" Style="{StaticResource lblError}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>