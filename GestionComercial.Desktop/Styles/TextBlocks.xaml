<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="BlueBold18" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Margin" Value="0,0,15,0"/>
        <Setter Property="Foreground" Value="#466AE0" />
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- Estilo para el contenido alineado a la izquierda -->    
    <Style x:Key="LeftAlignedTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Left"/>
    </Style>

    <!-- Estilo para el contenido centrado -->
    <Style x:Key="CenteredTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="12"/>
        <!--<Setter Property="Background" Value="{Binding Stock, Converter={StaticResource NegativoAFondoConverter}}"/>
        <Setter Property="Foreground" Value="{Binding Stock, Converter={StaticResource NegativoATextoConverter}}"/>-->

    </Style>


</ResourceDictionary>