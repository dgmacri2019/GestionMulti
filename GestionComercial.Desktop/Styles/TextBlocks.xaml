<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="BlackNormal10" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Margin" Value="5,0,0,0"/>
        <Setter Property="Foreground" Value="#3C3C3C" />
        <Setter Property="FontWeight" Value="Normal"/>
    </Style>
    
    <Style x:Key="BlueBold10" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Margin" Value="0,0,5,0"/>
        <Setter Property="Foreground" Value="#0E3CE1" />
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>
    
    <Style x:Key="BlueBold20" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Margin" Value="0,0,15,0"/>
        <Setter Property="Foreground" Value="#0E3CE1" />
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style x:Key="BlueBold24" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="Margin" Value="0,0,15,0"/>
        <Setter Property="Foreground" Value="#0E3CE1" />
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>


    <!-- Estilo para el contenido alineado a la izquierda -->    
    <Style x:Key="LeftAlignedTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Left"/>
    </Style>

    <!-- Estilo para el contenido centrado -->
    <Style x:Key="CenteredTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="12"/>
        <!--<Setter Property="Background" Value="{Binding Stock, Converter={StaticResource NegativoAFondoConverter}}"/>
        <Setter Property="Foreground" Value="{Binding Stock, Converter={StaticResource NegativoATextoConverter}}"/>-->

    </Style>

    <Style x:Key="CambioColor" TargetType="TextBlock">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#008080"/>
                <Setter Property="FontSize" Value="15"/>
            </Trigger> 
            <Trigger Property="IsMouseOver" Value="False">
                <Setter Property="Foreground" Value="#606060"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>