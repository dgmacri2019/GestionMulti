<Window x:Class="GestionComercial.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" 
        Height="350" 
        Width="350" 
        Icon="/Images/090906_Tienda2.png"
        WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Usuario:" Style="{StaticResource BlueBold10}"/>
            <TextBox x:Name="txtUsername" Margin="0,5" KeyDown="txtUsername_KeyDown"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Password:" Style="{StaticResource BlueBold10}"/>
            <PasswordBox x:Name="txtPassword" Margin="0,5" KeyDown="txtPassword_KeyDown">               
            </PasswordBox>
        </StackPanel>

        <Button 
            x:Name="btnLogin"
            Grid.Row="2" 
            Content="Login" 
            Height="30" 
            Click="Login_Click"
            Style="{StaticResource BtnLogin}"/>

        <TextBlock 
            x:Name="lblError"
            Grid.Row="3"
            Style="{StaticResource lblError}"
            TextWrapping="Wrap"/>
    </Grid>
</Window>
