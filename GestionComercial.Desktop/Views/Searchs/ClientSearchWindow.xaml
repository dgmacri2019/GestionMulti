<Window x:Class="GestionComercial.Desktop.Views.Searchs.ClientSearchWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestionComercial.Desktop.Views.Searchs"
        Title="Buscar cliente" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>


    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>      

        <!-- Caja de búsqueda -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <TextBlock Text="Buscar: " VerticalAlignment="Center"/>
            <TextBox x:Name="txtSearch" Width="300" Margin="5 0 0 0"
                  KeyUp="txtSearch_KeyUp"/>
        </StackPanel>

        <!-- Grilla de artículos -->
        <DataGrid x:Name="dgClients" Grid.Row="1" 
               AutoGenerateColumns="False"
               IsReadOnly="True"
               SelectionMode="Single"
               MouseDoubleClick="dgClients_MouseDoubleClick">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding OptionalCode}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Razón Social" Binding="{Binding BusinessName}" Width="*"/>
                <DataGridTextColumn Header="Nombre Fantasía" Binding="{Binding FantasyName}" Width="*"/>
                <DataGridTextColumn Header="DNI / CUIT" Binding="{Binding DocumentNumber}" Width="auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botones -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Aceptar" Width="80" Margin="0 0 5 0" Click="BtnOk_Click"/>
            <Button Content="Cancelar" Width="80" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
