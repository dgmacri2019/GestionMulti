<Window x:Class="GestionComercial.Desktop.Views.Sales.PayMethodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestionComercial.Desktop.Views.Sales"
        mc:Ignorable="d"
        Title="Metodo de Pago" Height="450" Width="600"
        x:Name="PayMethodWin">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="dgMetodosPago"
                  AutoGenerateColumns="False"
                  ItemsSource="{Binding MetodosPago}"
                  CanUserAddRows="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Método" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Metodo}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.AvailableMetodos, RelativeSource={RelativeSource AncestorType=Window}}"
                      SelectedItem="{Binding Metodo, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Caja" Binding="{Binding Caja}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat={}{0:C2}}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right">
            <Button Content="Aceptar" Width="100" Margin="5" Click="Aceptar_Click"/>
            <Button Content="Cancelar" Width="100" Margin="5" Click="Cancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
